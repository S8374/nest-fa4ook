info:
  name: reset-password
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/reset-password"
  body:
    type: json
    data: |
      {
      //   "exchangeToken": "fiamanillahb2+3@gmail.com",
        "newPassword": "Password1232!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
