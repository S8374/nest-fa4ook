info:
  name: delete
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/property-attributes/create"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
