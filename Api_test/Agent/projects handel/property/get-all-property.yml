info:
  name: get-all-property
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/property/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
