info:
  name: payment-plans-all
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-plans/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
