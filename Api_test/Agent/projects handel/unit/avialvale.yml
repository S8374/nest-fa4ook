info:
  name: avialvale
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/units/available"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
