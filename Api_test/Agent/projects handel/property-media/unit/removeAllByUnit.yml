info:
  name: removeAllByUnit
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/media/property/{{unitId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
